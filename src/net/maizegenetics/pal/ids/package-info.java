/**
 * Taxa (sample) identifiers, names, pedigree tracking.
 * 
 * {@link Identifier} is the wrapping the taxa name that allow names to have several
 * name levels (e.g. name:variety:species:genus)
 * <p>
 * {@link IdGroup} are collections of Identifiers that form the basis of an {@link Alignment}
 * 
 */
package net.maizegenetics.pal.ids;
